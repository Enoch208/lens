# Design reference for the UI, not part of this product (see commit 6586094 for its contents)
frontend/

# dependencies
node_modules/
.pnp
.pnp.*

# next.js
.next/
out/
build/

# env & credentials
.env*
!.env.example
*.local.json

# LENS runtime state — run transcripts and attempt counters, not verified artifacts
.lens/runs/
.lens/state/

# Seatline demo state — reseeded by /demo/reset, must never pollute the verified diff
data/

# film workspace
video/

# tooling scratch
.worktrees/
.superpowers/
scratch/
.DS_Store
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
coverage/
.vercel
*.pem

# Handoff prompt for the second agent — working note, not part of the product
AGENT-B-PROMPT.md

# Kane evidence packs — ~1MB each; the run share_url is the durable, linkable evidence
.testmuai/evidence/
AGENT-C-PROMPT.md
AGENT-V-PROMPT.md
